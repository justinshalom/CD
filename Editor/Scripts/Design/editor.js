$(document).ready(function() {
    $(window).on('message',
        function(e, v) {

        });
    $.get("http://localhost:53910", function (data) {
        //var ifrm = document.getElementById('editoriframe');
        //ifrm = ifrm.contentWindow || ifrm.contentDocument.document || ifrm.contentDocument;
        //ifrm.document.open();
        ////ifrm.document.write(data);
        //ifrm.document.close();
          
        });

   

});